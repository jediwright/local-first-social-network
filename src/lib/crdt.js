/**
 * crdt.js — CRDT merge helpers for Local-First Social
 *
 * Phase 4 placeholder.
 *
 * Will handle:
 *   - Thread Y.Array merge on initial connection between two devices
 *   - State vector exchange for efficient sync (send only missing updates)
 *   - Conflict resolution for simultaneous edits
 *
 * The Y.js CRDT guarantees eventual consistency — this module wraps
 * the merge protocol in the trust-tier context (only merge threads
 * with contacts at 'close' or 'contact' tier).
 */

// Phase 4 — not yet implemented
